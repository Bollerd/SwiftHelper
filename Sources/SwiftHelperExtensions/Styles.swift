//
//  Styles.swift
//  
//
//  Created by Dirk Boller on 11.11.21.
//

import SwiftUI
/*
@available(iOS 13.0, *)
public struct LibGradientButtonStyle: ButtonStyle {
    public func makeBody(configuration: Self.Configuration) -> some View {
        configuration.label
            .foregroundColor(Color.white)
            .padding()
            .background(LinearGradient(gradient: Gradient(colors: [Color.blue, Color.green]), startPoint: .leading, endPoint: .trailing))
          //  .background(configuration.isPressed ? Color.green : Color.yellow)
            .cornerRadius(15.0)
            .scaleEffect(configuration.isPressed ? 0.9 : 1.0)
    }
}

@available(iOS 13.0, *)
public struct LibBigGradientButtonStyle: ButtonStyle {
    public func makeBody(configuration: Self.Configuration) -> some View {
        configuration.label
            .foregroundColor(Color.white)
             .padding(.all, CGFloat(50))
            .background(LinearGradient(gradient: Gradient(colors: [Color.red, Color.orange]), startPoint: .leading, endPoint: .trailing))
          //  .background(configuration.isPressed ? Color.green : Color.yellow)
            .cornerRadius(15.0)
            .scaleEffect(configuration.isPressed ? 0.9 : 1.0)
    }
}

@available(iOS 13.0, *)
public struct LibOrangeRoundedBorderStyle : TextFieldStyle {
    public func _body(configuration: TextField<Self._Label>) -> some View {
    configuration
      .padding(7)
      .background(
        RoundedRectangle(cornerRadius: 10)
          .strokeBorder(Color.orange, lineWidth: 1)
    )
  }
}
*/
